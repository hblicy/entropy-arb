# live-trading requirements (transaction signing SDKs)
# 实盘依赖（交易签名 SDK）
-r requirements.txt

# Hyperliquid signing (Entropy leg, and the tradexyz hedge)
hyperliquid-python-sdk==0.24.0
eth-account==0.13.7

# Lighter signing (only needed with --hedge lighter / lighter-rh).
# Pinned to a verified main-branch commit for reproducible deployment.
# 仅当 --hedge 为 lighter / lighter-rh 时需要；固定到已验证提交，避免重装漂移。
lighter-sdk @ git+https://github.com/elliottech/lighter-python.git@ebc50660efc99f31e4055418e4514255456cb060
